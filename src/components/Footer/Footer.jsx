import {FooterComponent} from "./Footer.styled"

const Footer = () => {
    return (
      <FooterComponent>
        {/* <NavBar />
        {isLoggedIn ? <UserMenu /> : <AuthNav />} */}
      </FooterComponent>
    );
  };
  export default Footer;